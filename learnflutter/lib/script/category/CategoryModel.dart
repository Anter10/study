class CategoryModel{
      String id;
      String title;
      String className;
      String msg;
      String msgInfo;
      String dateTime;
      String authorName;

      CategoryModel(this.id, this.title, this.className, this.msg, this.msgInfo, this.dateTime, this.authorName);
}